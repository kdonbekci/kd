import React, { Fragment } from 'react';
import { NavLink } from 'react-router-dom';

const CollapsedSidebar = (props) => {
    return (
        <section id='collapsed-sidebar'>
            <h1>CollapsedSidebar</h1>
        </section>
    );
};

export default CollapsedSidebar;