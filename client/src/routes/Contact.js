import React, { Fragment } from 'react';
import { BioSidebar } from '../components/layout'

const Contact = (props) => {
    return (
        <Fragment>
            <BioSidebar />
            <section id='content'>
                <h1>Contact page</h1>
            </section>
        </Fragment>
    );
};

export default Contact;